# origin-plugin-monorepo

Origin plugin SDK and first-party plugins — npm workspaces monorepo.

At session start: `get_context(tags: ["origin-project", "origin-plugin-monorepo"])`

## Conventions

- npm workspaces monorepo
- TypeScript strict, Vite for plugin builds
- Changesets for versioning and publishing
